BASE_DIR=$(cd $(dirname ${BASH_SOURCE[0]})/.. && pwd)
BIN_DIR=$BASE_DIR/bin
CONFIG_DIR=$BASE_DIR/etc
LIBEXEC_DIR=$BASE_DIR/libexec
COMMAND_DIR=$LIBEXEC_DIR/cfdns

source $LIBEXEC_DIR/functions
source $CONFIG_DIR/config

# vim: ft=bash
